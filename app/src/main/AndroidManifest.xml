<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sda.balys.robert.weeding">
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <application
        android:name=".AndroidApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.MenuActivity">
        </activity>
        <activity android:name=".view.ExpanseActivity"
            android:theme="@style/NoToolbar"
            android:label="Wydatki">

        </activity>
        <activity android:name=".view.VisitorsActivity"
            android:theme="@style/NoToolbar"
            android:label="Goście">
        </activity>
        <activity android:name=".view.ContactActivity"
            android:label="Kontakty">

        </activity>
    </application>

</manifest>